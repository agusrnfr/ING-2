<%- include ('header', {tituloweb: '¡Oh my dog!'}); %>

<body id="body">
<card>

<div class="card" style="width: 30rem;margin: 5px;">
    <div class="card-body">
    <h5 class="card-title"> <h3>Inicio</h3></h5>
    <p class="card-text">

        <% if (typeof session.usuario !== 'undefined' && (session.loggedin === true)) { %>
            <h5>Bienvenido: <strong><%= session.usuario.mail %></strong></h5>
        <% } else { %>
            <h5>Bienvenido <strong>visitante</strong></h5>
        <% } %>


<% if (typeof session.usuario !== 'undefined' && (session.loggedin === true)) { %>
    <div>
    <form id="logout-form" action="/logout" method="POST">
        <button type="submit" class="btn btn-link">Cerrar sesión</button>
    </form>
    </div>
<% } else { %>

    <div>
        <a href="/login">login</a>
    </div>

<% } %>

<div>
    ________________ 
</div>

<div>
    <a href="/adopciones">Adopcion de perros</a>
</div>
<div>
    <a href="/campanias">Campañas</a>
</div>

<% if (typeof session.usuario !== 'undefined' && (session.loggedin === true)) { %>
<div>
              Mi Perfil
</div>

<div>
    <a href="/modificar_mi_perfil">Modificar mi perfil</a>
</div>

<div>
    <a href="/ver_mascota/<%= session.usuario.id %>">Mis mascotas</a>
</div>

<div>
    <a href="/historial">Historial de visitas</a>
</div>

<div>
    <a href="/libreta_sanitaria">Libreta</a>
</div>


<div>
    <a href="/turnos">Solicitar un turno</a>
</div>
<div>
    <a href="/turnos/misTurnos">Listar mis turnos</a>
</div>
<div>
    <a href="/ver_cliente/cupones/<%= session.usuario.id %>">Ver mis beneficios</a>
</div>
<% } %>


<% if (typeof session.usuario !== 'undefined' && (session.loggedin === true) &&(session.usuario.rol === 'admin')) { %>
<div>
    ________admin only__________ 
</div>

<div>
    <a href="/table">Lista de clientes</a>
</div>

<div>
    <a href="/register">Registrar a un cliente</a>
</div>

<div>
    <a href="/turnos/listarTodosLosTurnos">Listar turnos de todos los clientes</a>
</div>
<% } %>

</p>
 </div>
</div>

</body>